/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["62.146.228.61", "localhost", "smartinvest.global"],
  },
};

module.exports = nextConfig;
